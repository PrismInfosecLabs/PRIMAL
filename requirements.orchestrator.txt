# Main orchestrator requirements (no ClamAV)
flask==2.3.3
yara-python==4.3.1
pefile==2023.2.7
python-magic==0.4.27
requests==2.31.0

# Flask dependencies
werkzeug==2.3.7
jinja2==3.1.2
click==8.1.7
itsdangerous==2.1.2
markupsafe==2.1.3
blinker==1.6.3

# HTTP client for container communication
httpx==0.24.1

# Database (if using SQLite with additional features)
# sqlite3 is built-in to Python

# Additional security and utility libraries
cryptography>=41.0.0  # For secure token generation
bleach>=6.0.0         # For input sanitization
validators>=0.22.0    # For input validation